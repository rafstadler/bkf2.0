import { ForT } from '../types';
import { Easing } from '@popmotion/easing';
declare type Keyframes = number[] | string[];
export interface KeyframesConfig {
    values: Keyframes;
    times?: number[];
    duration?: number;
    ease?: Easing | Easing[];
}
export declare const keyframes: ({ values, times, duration, ease }: KeyframesConfig) => ForT;
export {};
