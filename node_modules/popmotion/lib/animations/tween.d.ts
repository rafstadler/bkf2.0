import { ForT } from '../types';
export interface TweenConfig {
    from?: number;
    to?: number;
    duration?: number;
    ease?: (t: number) => number;
}
export declare const tween: ({ from, to, duration, ease }?: TweenConfig) => ForT;
