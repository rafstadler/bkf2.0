import { ForT } from '../types';
interface DecayConfig {
    from?: number;
    velocity?: number;
    power?: number;
    timeConstant?: number;
    modifyTarget?: (target: number) => number;
}
export declare const decay: ({ velocity, from, power, timeConstant, modifyTarget }: DecayConfig) => ForT;
export {};
