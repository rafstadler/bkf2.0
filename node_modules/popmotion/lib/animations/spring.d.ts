import { ForT } from '../types';
export interface SpringConfig {
    from?: number;
    to?: number;
    velocity?: number;
    stiffness?: number;
    damping?: number;
    mass?: number;
}
export declare const spring: ({ from, to, velocity, stiffness, damping, mass }: SpringConfig) => ForT;
