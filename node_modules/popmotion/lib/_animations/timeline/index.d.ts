import { Instruction } from './types';
import { TweenProps } from '../tween/types';
declare const timeline: <V = any>(instructions: Instruction[], { duration, elapsed, ease, loop, flip, yoyo }?: TweenProps) => any;
export default timeline;
